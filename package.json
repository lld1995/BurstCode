{
  "name": "burstcode",
  "displayName": "BurstCode",
  "description": "Precise, Fast, Cheap, Secure — an autonomous coding agent powered entirely by your own local, OpenAI-compatible LLMs.",
  "version": "0.1.132",
  "publisher": "burstcode",
  "repository": {
    "type": "git",
    "url": "https://github.com/lld1995/BurstCode.git"
  },
  "homepage": "https://github.com/lld1995/BurstCode#readme",
  "bugs": {
    "url": "https://github.com/lld1995/BurstCode/issues"
  },
  "license": "MIT",
  "icon": "media/logo.png",
  "engines": {
    "vscode": "^1.106.0"
  },
  "categories": [
    "Programming Languages",
    "Machine Learning",
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "burstcode",
          "title": "BurstCode",
          "icon": "media/icon.svg"
        }
      ],
      "secondarySidebar": [
        {
          "id": "burstcode-chat",
          "title": "BurstCode Chat",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "burstcode": [
        {
          "id": "burstcode.basicInfoView",
          "name": "BurstCode",
          "icon": "media/icon.svg"
        }
      ],
      "burstcode-chat": [
        {
          "id": "burstcode.chatView",
          "name": "BurstCode Chat",
          "type": "webview",
          "icon": "media/icon.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "burstcode.openChat",
        "title": "BurstCode: Open Chat",
        "icon": "media/logo.png"
      },
      {
        "command": "burstcode.newChat",
        "title": "BurstCode: New Chat",
        "icon": "$(add)"
      },
      {
        "command": "burstcode.configureModel",
        "title": "BurstCode: Configure Model"
      },
      {
        "command": "burstcode.toggleSetting",
        "title": "BurstCode: Toggle a Boolean Setting"
      },
      {
        "command": "burstcode.acceptAllSuggestions",
        "title": "BurstCode: Accept All Pending Suggestions"
      },
      {
        "command": "burstcode.rejectAllSuggestions",
        "title": "BurstCode: Reject All Pending Suggestions"
      },
      {
        "command": "burstcode.acceptHunk",
        "title": "BurstCode: Accept Hunk"
      },
      {
        "command": "burstcode.rejectHunk",
        "title": "BurstCode: Reject Hunk"
      },
      {
        "command": "burstcode.selectModel",
        "title": "BurstCode: Select Active Model"
      },
      {
        "command": "burstcode.selectLanguage",
        "title": "BurstCode: Select Side Panel Language"
      },
      {
        "command": "burstcode.openGlobalRules",
        "title": "BurstCode: Open Global Rules"
      },
      {
        "command": "burstcode.openGlobalSkills",
        "title": "BurstCode: Open Global Skills Folder"
      },
      {
        "command": "burstcode.restoreCheckpoint",
        "title": "BurstCode: Restore Git Checkpoint"
      },
      {
        "command": "burstcode.background.toggle",
        "title": "BurstCode: Toggle Background Code Explorer"
      },
      {
        "command": "burstcode.background.runOnce",
        "title": "BurstCode: Run Background Analysis Now"
      },
      {
        "command": "burstcode.background.selectModel",
        "title": "BurstCode: Select Background Explorer Model"
      },
      {
        "command": "burstcode.background.showReport",
        "title": "BurstCode: Show Background Explorer Report"
      },
      {
        "command": "burstcode.background.resetState",
        "title": "BurstCode: Reset Background Explorer State"
      },
      {
        "command": "burstcode.background.showActivityLog",
        "title": "BurstCode: Show Background Explorer Activity Log"
      },
      {
        "command": "burstcode.background.menu",
        "title": "BurstCode: Background Explorer Menu"
      },
      {
        "command": "burstcode.web.testBrave",
        "title": "BurstCode: Test Brave Search API Key"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "burstcode.toggleSetting",
          "when": "false"
        }
      ],
      "editor/title": [
        {
          "command": "burstcode.openChat",
          "group": "navigation@1"
        }
      ],
      "view/title": [
        {
          "command": "burstcode.openChat",
          "when": "view == burstcode.basicInfoView",
          "group": "navigation@1"
        },
        {
          "command": "burstcode.newChat",
          "when": "view == burstcode.chatView",
          "group": "navigation@1"
        }
      ]
    },
    "configuration": {
      "title": "BurstCode",
      "properties": {
        "burstcode.llm.chat.baseURL": {
          "type": "string",
          "default": "http://localhost:11434/v1",
          "description": "Chat profile · OpenAI-compatible base URL (e.g. http://localhost:11434/v1)."
        },
        "burstcode.llm.chat.apiKey": {
          "type": "string",
          "default": "",
          "description": "Chat profile · API key (optional for most local servers)."
        },
        "burstcode.llm.chat.model": {
          "type": "string",
          "default": "qwen2.5-coder:7b",
          "description": "Chat profile · active model id used for chat requests."
        },
        "burstcode.llm.chat.temperature": {
          "type": "number",
          "default": 0.2,
          "description": "Chat profile · sampling temperature."
        },
        "burstcode.llm.chat.contextWindow": {
          "type": "number",
          "default": 131072,
          "description": "Chat profile · total context window in tokens."
        },
        "burstcode.llm.chat.allowSelfSignedCerts": {
          "type": "boolean",
          "default": false,
          "description": "Chat profile · skip TLS certificate verification for the baseURL."
        },
        "burstcode.llm.chat.models": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Chat profile · manually-curated model ids shown in the picker alongside ids fetched from /v1/models."
        },
        "burstcode.llm.background.inherit": {
          "type": "boolean",
          "default": false,
          "order": 20,
          "markdownDescription": "**Background profile** · When enabled, the Background Explorer uses the same endpoint/model as the chat profile. Leave disabled and set `#burstcode.llm.background.baseURL#` to use a separate endpoint."
        },
        "burstcode.llm.background.baseURL": {
          "type": "string",
          "default": "",
          "order": 21,
          "description": "Background profile · OpenAI-compatible base URL. Leave empty to inherit from the chat profile when `inherit` is false."
        },
        "burstcode.llm.background.apiKey": {
          "type": "string",
          "default": "",
          "order": 22,
          "description": "Background profile · API key. Leave empty to inherit from the chat profile when `inherit` is false."
        },
        "burstcode.llm.background.model": {
          "type": "string",
          "default": "",
          "order": 23,
          "description": "Background profile · model id used for background analysis. Leave empty to fall back to the first entry of `models`, then the chat model."
        },
        "burstcode.llm.background.temperature": {
          "type": "number",
          "default": 0.2,
          "order": 24,
          "description": "Background profile · sampling temperature."
        },
        "burstcode.llm.background.contextWindow": {
          "type": "number",
          "default": 131072,
          "order": 25,
          "description": "Background profile · total context window in tokens."
        },
        "burstcode.llm.background.allowSelfSignedCerts": {
          "type": "boolean",
          "default": false,
          "order": 26,
          "description": "Background profile · skip TLS certificate verification for the baseURL."
        },
        "burstcode.llm.background.models": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "order": 27,
          "description": "Background profile · manually-curated model ids for the picker."
        },
        "burstcode.llm.image.model": {
          "type": "string",
          "default": "",
          "order": 30,
          "markdownDescription": "**Image profile** · model id used by the `generate_image` tool, e.g. `gpt-image-1`, `gpt-image-2`, `dall-e-3`. These are called via `/v1/images/generations` — they CANNOT be used as the chat model. Leave empty to default to `gpt-image-1`."
        },
        "burstcode.llm.image.baseURL": {
          "type": "string",
          "default": "",
          "order": 31,
          "description": "Image profile · OpenAI-compatible base URL for image generation. Leave empty to inherit from the chat profile."
        },
        "burstcode.llm.image.apiKey": {
          "type": "string",
          "default": "",
          "order": 32,
          "description": "Image profile · API key for the image endpoint. Leave empty to inherit from the chat profile."
        },
        "burstcode.llm.image.size": {
          "type": "string",
          "default": "1024x1024",
          "order": 33,
          "description": "Image profile · default image size (e.g. 1024x1024, 1024x1536, 1536x1024)."
        },
        "burstcode.llm.image.allowSelfSignedCerts": {
          "type": "boolean",
          "default": false,
          "order": 34,
          "description": "Image profile · skip TLS certificate verification for the image baseURL."
        },
        "burstcode.llm.video.model": {
          "type": "string",
          "default": "",
          "order": 35,
          "markdownDescription": "**Video profile** · model id used by the `generate_video` tool, e.g. `bytedance/doubao-seedance-2.0`, `sora-2`. Do not use text/chat models here. Called via OpenAI-compatible video endpoints (`POST /videos` relative to a `/v1` base URL). Leave empty to default to `seedance-1-0-pro-i2v`."
        },
        "burstcode.llm.video.baseURL": {
          "type": "string",
          "default": "",
          "order": 36,
          "description": "Video profile · OpenAI-compatible base URL for video generation. Usually ends with /v1. Leave empty to inherit from the chat profile."
        },
        "burstcode.llm.video.apiKey": {
          "type": "string",
          "default": "",
          "order": 37,
          "description": "Video profile · API key for the video endpoint. Leave empty to inherit from the chat profile."
        },
        "burstcode.llm.video.resolution": {
          "type": "string",
          "default": "1280x720",
          "order": 38,
          "description": "Video profile · default video size for OpenAI-compatible video endpoints (e.g. 1280x720, 720x1280, 1920x1080; 720p is also accepted and normalized)."
        },
        "burstcode.llm.video.duration": {
          "type": "string",
          "default": "5",
          "order": 39,
          "description": "Video profile · default video duration in seconds (e.g. 5, 10)."
        },
        "burstcode.llm.video.allowSelfSignedCerts": {
          "type": "boolean",
          "default": false,
          "order": 40,
          "description": "Video profile · skip TLS certificate verification for the video baseURL."
        },
        "burstcode.mcp.servers": {
          "type": "array",
          "order": 39,
          "default": [],
          "markdownDescription": "**MCP** · Model Context Protocol servers exposed as BurstCode tools. Multiple servers are connected and used together. Each entry requires `name` and either `command` (stdio) or `url` (HTTP/SSE). The standard `{ \"mcpServers\": { \"name\": { ... } } }` object form (as used by Claude Desktop / Cursor) is also accepted — the key becomes the server name. Configure which discovered MCP tools are enabled from the BurstCode left panel.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Short server name used as the MCP tool prefix."
              },
              "command": {
                "type": "string",
                "description": "Command for a stdio MCP server, e.g. npx, node, python."
              },
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "default": [],
                "description": "Arguments passed to command. Supports ${workspaceFolder}."
              },
              "env": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "default": {},
                "description": "Extra environment variables for stdio MCP servers. Values support ${workspaceFolder}."
              },
              "cwd": {
                "type": "string",
                "description": "Working directory for stdio MCP servers. Supports ${workspaceFolder}."
              },
              "url": {
                "type": "string",
                "description": "URL for a remote HTTP/SSE MCP server."
              },
              "headers": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                },
                "default": {},
                "description": "HTTP headers for remote MCP servers."
              },
              "disabled": {
                "type": "boolean",
                "default": false,
                "description": "Disable this MCP server without deleting its config."
              }
            },
            "required": [
              "name"
            ]
          }
        },
        "burstcode.mcp.enabledTools": {
          "type": "array",
          "order": 40,
          "default": [],
          "items": {
            "type": "string"
          },
          "markdownDescription": "**MCP** · Enabled MCP tool IDs selected from the BurstCode left panel. Empty means all discovered tools from configured MCP servers are enabled. Prefer using the left-panel **MCP 配置 → 启用的 MCP 工具…** picker instead of editing this manually."
        },
        "burstcode.web.proxyUrl": {
          "type": "string",
          "default": "",
          "order": 41,
          "description": "Web tools · HTTP/HTTPS proxy URL used by web_search and read_webpage. Leave empty to fall back to VS Code http.proxy, then HTTPS_PROXY/HTTP_PROXY."
        },
        "burstcode.web.braveApiKey": {
          "type": "string",
          "default": "",
          "order": 42,
          "markdownDescription": "**Web tools** · Brave Search API key. When configured, `web_search` tries Brave before DuckDuckGo/Bing."
        },
        "burstcode.ui.language": {
          "type": "string",
          "enum": [
            "zh",
            "en",
            "auto"
          ],
          "enumDescriptions": [
            "简体中文（默认）",
            "English",
            "跟随 VS Code 显示语言 / Follow VS Code display language"
          ],
          "default": "zh",
          "description": "Language for the BurstCode side panel (Models / Permissions / Background Explorer / Actions). The chat window stays English. 侧边面板的显示语言；聊天窗口始终为英文。"
        },
        "burstcode.ui.taskDoneSound": {
          "type": "boolean",
          "default": true,
          "order": 50,
          "description": "Play a repeating notification sound when an agent task completes. The sound stops on the first detected user activity, such as clicking, typing, focusing the window, or interacting with the BurstCode view. If VS Code is not focused on Windows, BurstCode finds the VS Code main window and invokes the native taskbar flash/highlight API, then posts a generic VS Code attention notification."
        },
        "burstcode.ui.taskDoneSoundIntervalMs": {
          "type": "number",
          "default": 800,
          "minimum": 250,
          "order": 51,
          "description": "Interval in milliseconds between repeated task-completion notification sounds. The completion sound is a bright ascending chime, distinct from the ask-user prompt sound."
        },
        "burstcode.ui.askUserSound": {
          "type": "boolean",
          "default": true,
          "order": 52,
          "description": "Play a repeating prompt sound while BurstCode is waiting for your answer to an ask_user prompt. This uses a softer attention tone that is distinct from the task-completion sound. If VS Code is not focused on Windows, BurstCode finds the VS Code main window and invokes the native taskbar flash/highlight API, then posts a VS Code attention notification."
        },
        "burstcode.ui.askUserSoundIntervalMs": {
          "type": "number",
          "default": 1200,
          "minimum": 250,
          "order": 53,
          "description": "Interval in milliseconds between repeated ask-user prompt notification sounds."
        },
        "burstcode.git.autoCheckpoint": {
          "type": "boolean",
          "default": true,
          "description": "Before applying any BurstCode edit, snapshot the working tree to refs/burstcode/checkpoints/<timestamp> so the change can be rolled back later."
        },
        "burstcode.agent.maxIterations": {
          "type": "number",
          "default": 512,
          "description": "Maximum tool-calling iterations per user request."
        },
        "burstcode.agent.requireConfirmBeforeEdit": {
          "type": "boolean",
          "default": true
        },
        "burstcode.agent.autoContinueOnLength": {
          "type": "boolean",
          "default": true,
          "description": "When the LLM stops mid-turn because of the output token limit (finish_reason=length), automatically prompt it to continue instead of ending the turn."
        },
        "burstcode.agent.maxAutoContinues": {
          "type": "number",
          "default": 2,
          "description": "Maximum number of consecutive auto-continues before giving up. Resets whenever the model successfully calls a tool."
        },
        "burstcode.agent.autoResumeOnStreamError": {
          "type": "boolean",
          "default": true,
          "description": "When the LLM stream is interrupted by a transient error (network drop, server reset, HTTP 5xx, etc.), automatically retry the in-flight turn instead of ending the run with an error."
        },
        "burstcode.agent.maxAutoResumes": {
          "type": "number",
          "default": 2,
          "description": "Maximum number of consecutive auto-resumes after a stream interruption before surfacing the error. The counter resets after a successful streaming turn."
        },
        "burstcode.agent.maxStuckRepeats": {
          "type": "number",
          "default": 2,
          "description": "Number of CONSECUTIVE identical tool-call turns that must be observed before escalating to an interactive prompt. The 1st repeat (2 identical turns) always injects an automatic self-correction nudge; once the counter reaches this value (default: 3 identical turns) the agent asks you to continue, give a hint, or stop."
        },
        "burstcode.agent.autoContinueOnPrematureStop": {
          "type": "boolean",
          "default": true,
          "description": "When the model stops after tool use without a clear final answer, automatically continue the run instead of silently ending."
        },
        "burstcode.agent.maxPrematureStopContinues": {
          "type": "number",
          "default": 2,
          "description": "Maximum number of automatic continuations after suspicious early stops in one user request."
        },
        "burstcode.agent.subagentMaxIterations": {
          "type": "number",
          "default": 4,
          "description": "Maximum tool-calling iterations per launched sub-agent. Keep this low because sub-agents are summary workers, not full foreground agents."
        },
        "burstcode.agent.maxConcurrentSubagents": {
          "type": "number",
          "default": 2,
          "description": "Maximum number of sub-agents that may run concurrently inside one foreground agent request."
        },
        "burstcode.agent.maxSubagentTasksPerCall": {
          "type": "number",
          "default": 4,
          "description": "Maximum number of tasks accepted by one launch_subagent tool call."
        },
        "burstcode.agent.subagentTaskTimeoutMs": {
          "type": "number",
          "default": 1800000,
          "description": "Hard timeout in milliseconds for each launched sub-agent task before it is cancelled and returned as partial/timeout. Values below 1800000 are clamped to 30 minutes."
        },
        "burstcode.agent.enableWriteSubagents": {
          "type": "boolean",
          "default": true,
          "description": "Allow launch_subagent to run independent write tasks that can call propose_edit within explicit allowedFiles scopes."
        },
        "burstcode.diag.webviewProbe": {
          "type": "boolean",
          "default": false,
          "description": "Diagnostic only — when true, the BurstCode chat panel renders a red banner at the bottom listing the hit-test result of every interactive button (probes for invisible overlays / unclickable controls) and logs every click. Reload the window after toggling. Leave off in normal use."
        },
        "burstcode.lsp.maxWaitMs": {
          "type": "number",
          "default": 60000,
          "description": "Maximum time to wait for a language server to be ready."
        },
        "burstcode.ripgrepPath": {
          "type": "string",
          "default": "",
          "description": "Full path to a ripgrep (rg / rg.exe) binary used by the grep_search and collect_context tools. Leave empty to auto-detect from the VS Code install and your system PATH. Set this if grep_search reports 'spawn rg ENOENT' / 'could not locate a ripgrep binary'. Reload the window after changing."
        },
        "burstcode.context.outlineBaseDepth": {
          "type": "number",
          "default": 2,
          "description": "Recursion depth used when generating the workspace outline injected into the agent's system prompt. Higher = more files visible but more tokens spent."
        },
        "burstcode.context.outlineSrcDepth": {
          "type": "number",
          "default": 4,
          "description": "Recursion depth applied to source-like directories (src, lib, app, packages, ...) so the model can see deeper into them."
        },
        "burstcode.context.outlineMaxBytes": {
          "type": "number",
          "default": 6000,
          "description": "Soft cap (in characters) on the workspace outline embedded in the system prompt. Outlines exceeding this are truncated and the agent is told to drill in via workspace_outline."
        },
        "burstcode.context.outlineExtraExcludes": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Additional directory basenames to exclude from the workspace outline (in addition to defaults like node_modules, dist, out, .git)."
        },
        "burstcode.background.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the background Code Explorer. When the IDE is idle (no chat run in progress and no recent edits), BurstCode will pick one source file at a time, read it, and write a per-file summary, suspected-bug log, and auto-generated unit tests for uncertain points to the workspace's output directory. Pauses automatically while the user is typing or while a chat request is being processed."
        },
        "burstcode.background.idleThresholdMs": {
          "type": "number",
          "default": 10000,
          "description": "Idle gap (ms) after the latest real text edit before a background analysis cycle starts. Cursor/editor/window changes only pause an already-running background job; they do not keep postponing the next cycle."
        },
        "burstcode.background.minIntervalMs": {
          "type": "number",
          "default": 30000,
          "description": "Minimum gap (ms) between successive background cycles, even when the user has been idle the whole time."
        },
        "burstcode.background.batchCharBudget": {
          "type": "number",
          "default": 120000,
          "description": "Soft character cap per full-scan batch. The background explorer enumerates all candidate source files, sorts by directory, and greedy-packs them into batches whose combined contents stay under this budget (one LLM call per batch). Roughly 4 chars ≈ 1 token; tune to your model's context window. Lower this for small-context models; raise it for big-context models to make each cycle do more work in fewer calls."
        },
        "burstcode.background.batchMaxFiles": {
          "type": "number",
          "default": 25,
          "description": "Hard cap on files per full-scan batch. Prevents pathological cases where many tiny files would otherwise pile into a single batch and slow down per-batch latency."
        },
        "burstcode.background.maxConcurrentTopics": {
          "type": "number",
          "default": 10,
          "description": "How many investigation topics to run in parallel within a single cycle. Each topic uses its own AgentLoop and therefore its own LLM streams. Increase to use the full cycle window when your endpoint allows multiple concurrent requests; lower to 1 for strictly sequential execution."
        },
        "burstcode.background.includeExtensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "ts",
            "tsx",
            "js",
            "jsx",
            "mjs",
            "cjs",
            "py",
            "go",
            "rs",
            "java",
            "kt",
            "kts",
            "c",
            "cc",
            "cpp",
            "h",
            "hpp",
            "cs",
            "rb",
            "php",
            "swift",
            "scala",
            "lua",
            "dart"
          ],
          "description": "File extensions (no leading dot) the background explorer is allowed to read and analyse."
        },
        "burstcode.background.maxFileBytes": {
          "type": "number",
          "default": 120000,
          "description": "Skip files larger than this many bytes. Keeps each LLM call from blowing past the model's context window."
        },
        "burstcode.background.outputDir": {
          "type": "string",
          "default": ".burstcode",
          "description": "Workspace-relative directory where the explorer writes docs/, bugs.md, tests/, and state.json. Created on first run; safe to delete."
        },
        "burstcode.background.perFileTimeoutMs": {
          "type": "number",
          "default": 300000,
          "description": "Base timeout (ms) for a single background LLM call (default 5 min). Full-scan batches and topic investigations both run an entire request and use 4× this value (default 20 min) so a slow batch / multi-file agent loop doesn't get killed prematurely. Raise this if you're driving the explorer with a small / slow / CPU-only model."
        },
        "burstcode.background.runGeneratedTests": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, the background explorer will spawn the appropriate test runner (vitest/jest for TS/JS via npx, pytest for Python) on each generated unit test file and record pass/fail/skip results to verifications.md and a per-test .result.md. Off by default — running auto-generated code touches your shell and dependencies; review the test files first before enabling on untrusted projects."
        },
        "burstcode.background.testRunTimeoutMs": {
          "type": "number",
          "default": 60000,
          "description": "Per-test execution timeout (ms). The runner is killed and recorded as 'error: timed out' if it exceeds this duration."
        },
        "burstcode.shell.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Allow the agent to execute shell commands via the run_shell tool. Disable to remove the tool from the agent's toolbox entirely (existing chats won't be able to spawn processes)."
        },
        "burstcode.shell.autoApprove": {
          "type": "boolean",
          "default": false,
          "description": "Skip the per-command approval prompt and execute every run_shell call immediately. Convenient for trusted local workflows; LEAVE OFF unless you understand the risk — the agent runs with the same privileges as VS Code."
        },
        "burstcode.shell.defaultTimeoutMs": {
          "type": "number",
          "default": 60000,
          "description": "Default timeout (ms) for a single run_shell invocation when the model does not specify one. Hard cap is 600000 (10 minutes). Processes that exceed the timeout are killed and the call returns isError=true."
        },
        "burstcode.shell.maxOutputBytes": {
          "type": "number",
          "default": 131072,
          "description": "Maximum bytes of stdout / stderr (each, separately) captured per run_shell call before the tail is dropped. Output is further compressed before being stored in context (see burstcode.agent). Raise this if you routinely redirect large build logs; lower it on memory-constrained machines. Hard cap is 524288 (512 KB)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "node esbuild.js --production",
    "vsix:main": "vsce package --no-dependencies --allow-missing-repository --skip-license",
    "vsix:local-alerts": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"Push-Location companion; try { ..\\node_modules\\.bin\\vsce.cmd package --no-dependencies --allow-missing-repository --skip-license --out ..\\burstcode-local-alerts.vsix } finally { Pop-Location }\"",
    "vsix": "npm run pack",
    "pack": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/pack.ps1",
    "compile-tests": "tsc -p ./",
    "lint": "eslint src companion/src --ext ts",
    "test": "npm run compile-tests && node --test out/test/runTest.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "diff": "^5.2.0",
    "gpt-tokenizer": "^2.5.1",
    "openai": "^4.71.0"
  },
  "devDependencies": {
    "@types/diff": "^5.2.1",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.5"
  }
}
