{
  "name": "burstcode",
  "displayName": "BurstCode",
  "description": "Windsurf-style autonomous coding agent powered by local OpenAI-compatible LLMs.",
  "version": "0.1.2",
  "publisher": "burstcode",
  "repository": {
    "type": "git",
    "url": "https://github.com/lld1995/BurstCode.git"
  },
  "homepage": "https://github.com/lld1995/BurstCode#readme",
  "bugs": {
    "url": "https://github.com/lld1995/BurstCode/issues"
  },
  "license": "MIT",
  "icon": "media/logo.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Machine Learning",
    "Other"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "burstcode",
          "title": "BurstCode",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "burstcode": [
        {
          "id": "burstcode.chatView",
          "name": "BurstCode Chat",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "burstcode.newChat",
        "title": "BurstCode: New Chat"
      },
      {
        "command": "burstcode.configureModel",
        "title": "BurstCode: Configure Model"
      },
      {
        "command": "burstcode.acceptAllSuggestions",
        "title": "BurstCode: Accept All Pending Suggestions"
      },
      {
        "command": "burstcode.rejectAllSuggestions",
        "title": "BurstCode: Reject All Pending Suggestions"
      },
      {
        "command": "burstcode.acceptHunk",
        "title": "BurstCode: Accept Hunk"
      },
      {
        "command": "burstcode.rejectHunk",
        "title": "BurstCode: Reject Hunk"
      },
      {
        "command": "burstcode.selectModel",
        "title": "BurstCode: Select Active Model"
      },
      {
        "command": "burstcode.restoreCheckpoint",
        "title": "BurstCode: Restore Git Checkpoint"
      },
      {
        "command": "burstcode.background.toggle",
        "title": "BurstCode: Toggle Background Code Explorer"
      },
      {
        "command": "burstcode.background.runOnce",
        "title": "BurstCode: Run Background Analysis Now"
      },
      {
        "command": "burstcode.background.selectModel",
        "title": "BurstCode: Select Background Explorer Model"
      },
      {
        "command": "burstcode.background.showReport",
        "title": "BurstCode: Show Background Explorer Report"
      },
      {
        "command": "burstcode.background.resetState",
        "title": "BurstCode: Reset Background Explorer State"
      },
      {
        "command": "burstcode.background.showActivityLog",
        "title": "BurstCode: Show Background Explorer Activity Log"
      },
      {
        "command": "burstcode.background.menu",
        "title": "BurstCode: Background Explorer Menu"
      }
    ],
    "configuration": {
      "title": "BurstCode",
      "properties": {
        "burstcode.llm.baseURL": {
          "type": "string",
          "default": "http://localhost:11434/v1",
          "description": "OpenAI-compatible base URL (Ollama / LM Studio / vLLM)."
        },
        "burstcode.llm.apiKey": {
          "type": "string",
          "default": "ollama",
          "description": "API key (some local servers ignore this; leave default for Ollama)."
        },
        "burstcode.llm.model": {
          "type": "string",
          "default": "qwen2.5-coder:7b",
          "description": "Model name as exposed by your local server."
        },
        "burstcode.llm.contextWindow": {
          "type": "number",
          "default": 32768,
          "description": "Total context window of the model in tokens."
        },
        "burstcode.llm.temperature": {
          "type": "number",
          "default": 0.2
        },
        "burstcode.llm.allowSelfSignedCerts": {
          "type": "boolean",
          "default": false,
          "description": "Skip TLS certificate verification when talking to the configured baseURL. Enable only for trusted self-hosted endpoints with self-signed certificates."
        },
        "burstcode.llm.endpoints": {
          "type": "array",
          "default": [],
          "description": "List of OpenAI-compatible endpoints. Each entry is a remote server (baseURL + apiKey). Models for each endpoint can be fetched live via /v1/models from the chat panel's model picker, or supplemented manually in the 'models' field below. The chat panel shows endpoints grouped in the picker.",
          "items": {
            "type": "object",
            "required": [
              "name",
              "baseURL"
            ],
            "properties": {
              "name": {
                "type": "string",
                "description": "Display name shown in the model picker."
              },
              "baseURL": {
                "type": "string",
                "description": "OpenAI-compatible base URL (e.g. http://localhost:11434/v1)."
              },
              "apiKey": {
                "type": "string",
                "description": "API key (optional for most local servers)."
              },
              "contextWindow": {
                "type": "number",
                "description": "Total context window in tokens."
              },
              "temperature": {
                "type": "number"
              },
              "allowSelfSignedCerts": {
                "type": "boolean",
                "description": "Skip TLS certificate verification for this endpoint's baseURL."
              },
              "models": {
                "type": "array",
                "description": "Manually-curated model ids for this endpoint. The picker shows these in addition to anything fetched from /v1/models.",
                "items": {
                  "type": "string"
                },
                "default": []
              }
            }
          }
        },
        "burstcode.llm.activeEndpoint": {
          "type": "string",
          "default": "",
          "description": "Name of the currently active endpoint from burstcode.llm.endpoints. Set automatically when picking a model in the chat panel."
        },
        "burstcode.llm.activeModel": {
          "type": "string",
          "default": "",
          "description": "Currently active model id under the active endpoint. Set automatically when picking a model in the chat panel."
        },
        "burstcode.llm.profiles": {
          "type": "array",
          "default": [],
          "description": "[Deprecated] Legacy LLM profiles (endpoint+model combined). Still read for backward compatibility — each profile is migrated into a burstcode.llm.endpoints entry on the fly. Prefer burstcode.llm.endpoints for new setups.",
          "items": {
            "type": "object",
            "required": [
              "name",
              "model"
            ],
            "properties": {
              "name": {
                "type": "string"
              },
              "baseURL": {
                "type": "string"
              },
              "apiKey": {
                "type": "string"
              },
              "model": {
                "type": "string"
              },
              "contextWindow": {
                "type": "number"
              },
              "temperature": {
                "type": "number"
              },
              "allowSelfSignedCerts": {
                "type": "boolean"
              }
            }
          }
        },
        "burstcode.llm.activeProfile": {
          "type": "string",
          "default": "",
          "description": "[Deprecated] Legacy active-profile name. Used only as a fallback when burstcode.llm.activeEndpoint is empty."
        },
        "burstcode.git.autoCheckpoint": {
          "type": "boolean",
          "default": true,
          "description": "Before applying any BurstCode edit, snapshot the working tree to refs/burstcode/checkpoints/<timestamp> so the change can be rolled back later."
        },
        "burstcode.agent.maxIterations": {
          "type": "number",
          "default": 25,
          "description": "Maximum tool-calling iterations per user request."
        },
        "burstcode.agent.requireConfirmBeforeEdit": {
          "type": "boolean",
          "default": true
        },
        "burstcode.agent.autoContinueOnLength": {
          "type": "boolean",
          "default": true,
          "description": "When the LLM stops mid-turn because of the output token limit (finish_reason=length), automatically prompt it to continue instead of ending the turn."
        },
        "burstcode.agent.maxAutoContinues": {
          "type": "number",
          "default": 3,
          "description": "Maximum number of consecutive auto-continues before giving up. Resets whenever the model successfully calls a tool."
        },
        "burstcode.agent.maxStuckRepeats": {
          "type": "number",
          "default": 2,
          "description": "Number of CONSECUTIVE identical tool-call turns that must be observed before escalating to an interactive prompt. The 1st repeat (2 identical turns) always injects an automatic self-correction nudge; once the counter reaches this value (default: 3 identical turns) the agent asks you to continue, give a hint, or stop."
        },
        "burstcode.lsp.maxWaitMs": {
          "type": "number",
          "default": 60000,
          "description": "Maximum time to wait for a language server to be ready."
        },
        "burstcode.context.outlineBaseDepth": {
          "type": "number",
          "default": 2,
          "description": "Recursion depth used when generating the workspace outline injected into the agent's system prompt. Higher = more files visible but more tokens spent."
        },
        "burstcode.context.outlineSrcDepth": {
          "type": "number",
          "default": 4,
          "description": "Recursion depth applied to source-like directories (src, lib, app, packages, ...) so the model can see deeper into them."
        },
        "burstcode.context.outlineMaxBytes": {
          "type": "number",
          "default": 6000,
          "description": "Soft cap (in characters) on the workspace outline embedded in the system prompt. Outlines exceeding this are truncated and the agent is told to drill in via workspace_outline."
        },
        "burstcode.context.outlineExtraExcludes": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Additional directory basenames to exclude from the workspace outline (in addition to defaults like node_modules, dist, out, .git)."
        },
        "burstcode.background.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the background Code Explorer. When the IDE is idle (no chat run in progress and no recent edits), BurstCode will pick one source file at a time, read it, and write a per-file summary, suspected-bug log, and auto-generated unit tests for uncertain points to the workspace's output directory. Pauses automatically while the user is typing or while a chat request is being processed."
        },
        "burstcode.background.idleThresholdMs": {
          "type": "number",
          "default": 10000,
          "description": "Idle gap (ms) after the latest real text edit before a background analysis cycle starts. Cursor/editor/window changes only pause an already-running background job; they do not keep postponing the next cycle."
        },
        "burstcode.background.minIntervalMs": {
          "type": "number",
          "default": 30000,
          "description": "Minimum gap (ms) between successive background cycles, even when the user has been idle the whole time."
        },
        "burstcode.background.filesPerCycle": {
          "type": "number",
          "default": 1,
          "description": "How many files to analyse per cycle. Each file = one LLM call."
        },
        "burstcode.background.maxConcurrentTopics": {
          "type": "number",
          "default": 10,
          "description": "How many investigation topics to run in parallel within a single cycle. Each topic uses its own AgentLoop and therefore its own LLM streams. Increase to use the full cycle window when your endpoint allows multiple concurrent requests; lower to 1 for strictly sequential execution."
        },
        "burstcode.background.includeExtensions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "ts",
            "tsx",
            "js",
            "jsx",
            "mjs",
            "cjs",
            "py",
            "go",
            "rs",
            "java",
            "kt",
            "kts",
            "c",
            "cc",
            "cpp",
            "h",
            "hpp",
            "cs",
            "rb",
            "php",
            "swift",
            "scala",
            "lua",
            "dart"
          ],
          "description": "File extensions (no leading dot) the background explorer is allowed to read and analyse."
        },
        "burstcode.background.maxFileBytes": {
          "type": "number",
          "default": 120000,
          "description": "Skip files larger than this many bytes. Keeps each LLM call from blowing past the model's context window."
        },
        "burstcode.background.outputDir": {
          "type": "string",
          "default": ".burstcode",
          "description": "Workspace-relative directory where the explorer writes docs/, bugs.md, tests/, and state.json. Created on first run; safe to delete."
        },
        "burstcode.background.endpoint": {
          "type": "string",
          "default": "",
          "description": "Endpoint name (from burstcode.llm.endpoints) the background explorer should use. Empty = inherit the chat panel's currently active endpoint. Ignored when burstcode.background.baseURL is set."
        },
        "burstcode.background.baseURL": {
          "type": "string",
          "default": "",
          "description": "OpenAI-compatible base URL for the background explorer ONLY. When set, this fully decouples the background loop from the chat panel's endpoint — useful when the background work runs against a different / cheaper / local server. Empty = derive baseURL from burstcode.background.endpoint (or chat's active endpoint)."
        },
        "burstcode.background.apiKey": {
          "type": "string",
          "default": "",
          "description": "API key paired with burstcode.background.baseURL. Empty = inherit the resolved endpoint's apiKey (or 'ollama' for local servers)."
        },
        "burstcode.background.allowSelfSignedCerts": {
          "type": "boolean",
          "default": false,
          "description": "Skip TLS certificate verification for burstcode.background.baseURL. Only applies when burstcode.background.baseURL is set; otherwise the resolved endpoint's setting is inherited."
        },
        "burstcode.background.model": {
          "type": "string",
          "default": "",
          "description": "Model id the background explorer should use under its endpoint. Empty = the endpoint's first known model, falling back to chat's active model. Lets you pair the background loop with a smaller / cheaper model than the one you chat with."
        },
        "burstcode.background.temperature": {
          "type": "number",
          "default": 0.2,
          "description": "Sampling temperature for background analysis calls."
        },
        "burstcode.background.contextWindow": {
          "type": "number",
          "default": 0,
          "description": "Override the context window (tokens) used to size background prompts. 0 = inherit from the resolved endpoint."
        },
        "burstcode.background.perFileTimeoutMs": {
          "type": "number",
          "default": 180000,
          "description": "Cancel a single-file analysis if it has not finished within this many milliseconds."
        },
        "burstcode.background.runGeneratedTests": {
          "type": "boolean",
          "default": false,
          "description": "When enabled, the background explorer will spawn the appropriate test runner (vitest/jest for TS/JS via npx, pytest for Python) on each generated unit test file and record pass/fail/skip results to verifications.md and a per-test .result.md. Off by default — running auto-generated code touches your shell and dependencies; review the test files first before enabling on untrusted projects."
        },
        "burstcode.background.testRunTimeoutMs": {
          "type": "number",
          "default": 60000,
          "description": "Per-test execution timeout (ms). The runner is killed and recorded as 'error: timed out' if it exceeds this duration."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "node esbuild.js --production",
    "vsix": "vsce package --no-dependencies --allow-missing-repository --skip-license",
    "pack": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/pack.ps1",
    "compile-tests": "tsc -p ./",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "dependencies": {
    "diff": "^5.2.0",
    "gpt-tokenizer": "^2.5.1",
    "openai": "^4.71.0"
  },
  "devDependencies": {
    "@types/diff": "^5.2.1",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.9.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.5"
  }
}
