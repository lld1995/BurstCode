# BurstCode Local Alerts Companion

Local UI companion extension for BurstCode.

Install this VSIX on the local VS Code client when the main BurstCode extension runs in a Remote SSH / WSL / Dev Container extension host. It handles local sound and taskbar attention alerts that cannot reliably be triggered from the remote extension host.
